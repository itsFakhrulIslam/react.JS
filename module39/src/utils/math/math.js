const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
const modulus = (a, b) => a % b;

const welcomeToNewMemberLearnReactJsModule39 = (name) => {
  console.log(`Welcome to new member learn ReactJs module39, ${name}!`);
};

const sayHelloToNewMemberLearnReactJsModule39 = (name) => {
  console.log(`Hello to new member learn ReactJs module39, ${name}!`);
};

export {
  add,
  subtract,
  multiply,
  divide,
  modulus,
  welcomeToNewMemberLearnReactJsModule39 as greetNewMember,
  sayHelloToNewMemberLearnReactJsModule39 as sayHelloToNewMember,
};
